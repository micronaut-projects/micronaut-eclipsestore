The following example shows how to create implementations for this repository:

snippet::io.micronaut.eclipsestore.docs.CustomerRepository[]

[.lang-python]
NOTE: EclipseStore cannot persist Python objects yet, see the <<rootInstance, Root Instance>> section; the Python repository implementations in the following sections show the intended API.
